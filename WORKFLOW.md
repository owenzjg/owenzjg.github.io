# 新增小界面工作流

以后每创建一个独立小界面，按下面顺序完成。

1. 新界面使用独立仓库，先完成并验证主要功能。
2. 按 `STYLE_GUIDE.md` 检查视觉、文案和移动端表现。
3. 获得可访问地址；尚未发布时可临时填写仓库地址。
4. 在本主站运行登记命令：

```bash
pnpm add:page -- \
  --slug focus-clock \
  --title 专注时钟 \
  --english "Focus Clock" \
  --url https://example.com \
  --tags timer,productivity \
  --accent '#2457d6'
```

5. 检查 `projects.json`：最新页面应排在最前，并且 slug 不重复。
6. 构建主站，确认卡片可点击后，将新界面与主站的更新一起提交。

## 给 Codex 的固定要求

当任务是在本项目体系中“创建新的小页面”时，完成页面本身后还要：

- 在 `projects.json` 登记标题、中英文名、地址、日期、标签与强调色。
- 保持主页面现有视觉语言，不擅自增加营销文案。
- 如果新页面尚无公开地址，登记仓库地址并在发布后替换。
- 不因为只有一个新条目而重做主页面结构。
