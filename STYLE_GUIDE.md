# GitHub 小页面视觉规范

这套风格用于个人小界面、倒计时、工具页与实验页面。核心不是机械复制某个模板，而是保持一致的编辑气质。

## 视觉关键词

- 瑞士排版 / Editorial / Quiet confidence
- 大面积米白，黑色正文，仅保留一个高饱和强调色
- 超大衬线标题搭配等宽小字
- 细线分区、明确网格、大量留白
- 少圆角、少阴影，不使用玻璃拟态、发光球和模板式渐变

## 默认设计令牌

```css
:root {
  --paper: #f2efe7;
  --ink: #181816;
  --muted: #77736a;
  --line: rgba(24, 24, 22, 0.24);
  --signal: #d73322;
}
```

强调色可以随页面主题改变，但每个页面只使用一个。默认字体组合：

```css
/* 展示标题 */
font-family: "Bodoni 72", Didot, "Songti SC", serif;

/* 标签、日期、数据 */
font-family: "SFMono-Regular", "DM Mono", Menlo, monospace;
```

## 文案规则

- 首屏只保留名称、核心数据、必要标签与操作。
- 中文用于事实，英文用于层级、编号和氛围。
- 不写“开启新篇章”“探索无限可能”等空泛句子。
- 一个界面如果删掉一句话仍能理解，就删掉它。

## 交互规则

- 动画只用于入场、数字变化和状态切换，时长 150–650ms。
- 尊重 `prefers-reduced-motion`。
- 移动端先保证数字、标题和主要操作；次要标签允许折叠。
- hover 主要改变文字颜色、位移或下划线，不做大幅缩放。

## 交付检查

- 没有多余卡片、装饰性标签或解释文案。
- 中英文层级清楚，字体回退后仍可读。
- 375px 与桌面宽度均不横向溢出。
- 所有日期、链接和数据源可验证。
- 新页面已经登记到 `data/pages.json`。
